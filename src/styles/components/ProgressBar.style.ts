import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        height: 6,
        borderRadius: 3,
        overflow: 'hidden'
    },
    progressBar: {
        flex: 1
    }
});
